# Frontend Environment Variables

# API URL for development (local backend)
REACT_APP_DEV_API_URL=http://localhost:5000/api

# API URL for production (when deployed to Vercel)
# This will be overridden by Vercel environment variables in production
REACT_APP_PROD_API_URL=https://your-backend-url.com/api

# MongoDB Connection String (for local development)
MONGODB_URI=mongodb+srv://investment:1Password@cluster0.yihwhok.mongodb.net/investment

# JWT Secret (for local development)
JWT_SECRET=your_jwt_secret_key

# Server Port (for local development)
PORT=5000
